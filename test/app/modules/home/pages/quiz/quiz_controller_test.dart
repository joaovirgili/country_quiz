import 'package:flutter_modular/flutter_modular_test.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:country_quiz/app/modules/home/pages/quiz/quiz_controller.dart';
import 'package:country_quiz/app/modules/home/home_module.dart';

void main() {
  initModule(HomeModule());
  // QuizController quiz;
  //
  setUp(() {
    //     quiz = HomeModule.to.get<QuizController>();
  });

  group('QuizController Test', () {
    //   test("First Test", () {
    //     expect(quiz, isInstanceOf<QuizController>());
    //   });

    //   test("Set Value", () {
    //     expect(quiz.value, equals(0));
    //     quiz.increment();
    //     expect(quiz.value, equals(1));
    //   });
  });
}
